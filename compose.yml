services:
  goal:
    build:
      context: .
    image: goal:local
    network_mode: none
    working_dir: /workspace
    volumes:
      - type: bind
        source: .
        target: /workspace
    command: ["--help"]
