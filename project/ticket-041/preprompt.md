# Ticket preprompt

- **Task ID**: ticket-041
- **Task title**: Make governed delivery diagnose new-project layouts
- **Created**: 2026-08-12T08:52:57Z

Keep executable implementation outside this governance/evidence directory.
Distinguish the maintained new-project source hub from an adopted target; never
recommend installing a target `.governance` package into the hub itself.
Treat diagnostics JSON as data: accept only known v1/v2 shapes and resolve a v2
documentation link only inside the directory containing the catalog.
All verification and dry-run paths must be observably read-only.
Read a human-owned user-*.md file only when one exists.
The request to execute this work creates SESSION_EXECUTION_AUTHORIZATION;
proceed within the recorded intent without a redundant confirmation prompt.
Require new authority for destructive action, secrets, external coordination,
material objective expansion and trusted merge approval.
