# Authority

SESSION_EXECUTION_AUTHORIZATION: user explicitly requested repair and publication through goal -a, then continued the work. This integration ticket publishes the merged Goal repair. Use the workspace layout explicitly provided by the user for this session; current managed v4 layout was observed but the user-specified canonical external path takes precedence.
