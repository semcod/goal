# Ticket preprompt

- **Task ID**: ticket-012
- **Task title**: Refresh runtime and release dependencies
- **Created**: 2026-08-10T07:14:51Z

Keep executable implementation outside this governance/evidence directory.
Read a human-owned user-*.md file only when one exists.

Use Python 3.12 or newer consistently with the package metadata.  Resolve the
lockfile from declared constraints, run the complete local and Docker matrices,
and deliver only through the configured governed Goal workflow.  Treat every
downstream repository as a separate trust and write boundary.
