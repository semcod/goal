# Ticket preprompt

- **Task ID**: ticket-032
- **Task title**: Expose packaged governance check through Goal
- **Created**: 2026-08-11T17:24:45Z

Keep executable implementation outside this governance/evidence directory.
Read a human-owned user-*.md file only when one exists.
The request to execute this work creates SESSION_EXECUTION_AUTHORIZATION;
proceed within the recorded intent without a redundant confirmation prompt.
Require new authority for destructive action, secrets, external coordination,
material objective expansion and trusted merge approval.

Follow-up boundary: every `goal governance ...` command enters the main group
without implicit project/user configuration mutation. Delivery subcommands that
need policy continue to call `ensure_config()` explicitly in their callback.
