2026-08-10 ticket-024 validation evidence

- `pytest -q tests/test_delivery_integrity.py`: 9 passed.
- Related push/dependency/bootstrap/installer suite: 165 passed, 1 skipped.
- `pytest -q`: 521 passed, 2 skipped; Git HEAD remained unchanged.
- Ruff: new regression module passed; E9 syntax checks passed for all changed
  implementation/test files.
- Diagit UV selection: `uv sync --extra test`.
- `./project/governance-check.sh`: GOV-PASS, 0 errors, 0 warnings.
- Docker build: `goal-ticket-024:validation`, image
  `sha256:6a42109443c1d0f20150aa268396b0c8c1c966264d7cb27b3c06fc8268f3e17d`.
- Network-disabled container smoke checks: `goal --version` reported 2.1.292;
  `goal --help` completed successfully.
- Validation completed and the ticket entered `PUBLICATION` for governed
  `goal -a --delivery-mode pull-request` delivery.

During validation, sequential CLI tests exposed duplicate command registration:
importing `goal.push.commands` replaced the canonical command and could execute
a real workflow. The runs were stopped, all automatically generated version and
documentation changes were restored, and a regression now prevents replacement.

The first governed publication run was stopped before commit when bootstrap
updated the optional README cost badge despite the application-only boundary.
The existing `GOAL_SKIP_COSTS_BADGE` switch now also covers commit-phase badge
refresh, and the generated README diff was restored before retrying delivery.
The first retry then demonstrated that this Goal-only environment switch leaked
into the project's own tests. Delivery failed closed before commit; the test
stage now temporarily removes the switch and restores it afterward.
