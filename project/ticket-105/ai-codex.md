SESSION_EXECUTION_AUTHORIZATION: the user's “tak, zgadzam sie, wykonaj”
authorizes the previously proposed deterministic adoption automation.
Scope: implement and validate a first read-only planner in semcod/goal,
then use the declared protected delivery process. No direct merge, fleet
adoption, policy waiver, secrets access or shared deployment change.
